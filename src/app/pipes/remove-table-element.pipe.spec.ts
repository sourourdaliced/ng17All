import { RemoveTableElementPipe } from './remove-table-element.pipe';

describe('RemoveTableElementPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTableElementPipe();
    expect(pipe).toBeTruthy();
  });
});
